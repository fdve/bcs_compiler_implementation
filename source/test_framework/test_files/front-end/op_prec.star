int start() {
    // should evaluate to -19
    int a = 1 - 2 * ---3 + 4;

    // bool aaa = !-!true;

    // looks good
    // a += a + 3 / -(4 + -5);



    Return 0;

    /*

    bool f = false;
    bool t = true;

    // in the AST, t != t should be the deepest nested subtree
    // after that t && f
    // after that f || t
    bool b = f || t != t && f;

    // b = (f || ((t != t) && f))


    bool f = false;
    bool t = true;



    // < >= has highest precedence, after that !=, after that &&, after that ||
    bool e = t || 1 < 2 && 3 >= 2 || t != f;


    // int d = 10 - - 10 * - 20;

    // bool bb = t || !f && t;

    Return 0;

    */

}

int myname()  {
    int a = 0;
}
