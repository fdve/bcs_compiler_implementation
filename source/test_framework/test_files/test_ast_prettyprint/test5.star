int f(int a, int b) {
    Return a + b;
}

int start() {
    int abc = -10;

    f(10,20);

    int a = f((10 - 20) / -(-(-(-30 + (20 / -40)))), (234 - 2) / 3);

    While true {
        bool b = false;
        b = !(!(!(b)));
    }
    Return a;
}

bool b() {
    Return !(!(!(false)));
}

char c() {
    While b() {
        While b() {}
            bool fddddd = false;
            If b() {

            }
            Else {
                !b();
            }
    }
    Return 'c';
}
